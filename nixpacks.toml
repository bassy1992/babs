[phases.setup]
# Install Python 3.11 and pip
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "cd back && pip install --upgrade pip",
  "cd back && pip install -r requirements.txt"
]

[start]
# Use 'python' instead of 'python3' in start.sh
cmd = "bash start.sh"
